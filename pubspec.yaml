name: hivault
description: A sample command-line application.
version: 0.1.0
repository: https://github.com/ack43/hivault

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  hive_ce: ^2.11.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
